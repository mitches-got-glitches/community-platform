[project]
name = "community-platform-ansible"
version = "0.1.0"
description = "Python tooling for the Ansible IaC spine (pinned ansible-core, run via uv)"
requires-python = ">=3.11"
dependencies = [
    "ansible-core>=2.17,<2.18",
]
